<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data noupdate="1">

        <!-- Sequences for ohealth.patient -->
        <record id="seq_type_ohealth_patient" model="ir.sequence.type">
            <field name="name">OHealth Patient</field>
            <field name="code">ohealth.patient</field>
        </record>

        <record id="seq_ohealth_patient" model="ir.sequence">
            <field name="name">OHealth Patient</field>
            <field name="code">ohealth.patient</field>
            <field name="prefix">IP</field>
            <field name="padding">3</field>
        </record>
        
        <!-- Sequences for ohealth.appointment -->
        <record id="seq_type_ohealth_appointment" model="ir.sequence.type">
            <field name="name">OHealth Appointment</field>
            <field name="code">ohealth.appointment</field>
        </record>

        <record id="seq_ohealth_appointment" model="ir.sequence">
            <field name="name">OHealth Appointment</field>
            <field name="code">ohealth.appointment</field>
            <field name="prefix">AP</field>
            <field name="padding">3</field>
        </record>
        
        <!-- Sequences for ohealth.prescription.order -->
        <record id="seq_type_ohealth_prescription_order" model="ir.sequence.type">
            <field name="name">OHealth Prescription Order</field>
            <field name="code">ohealth.prescription.order</field>
        </record>

        <record id="seq_ohealth_prescription_order" model="ir.sequence">
            <field name="name">OHealth Prescription Order</field>
            <field name="code">ohealth.prescription.order</field>
            <field name="prefix">PRO</field>
            <field name="padding">3</field>
        </record>
		
		
    </data>
</openerp>
